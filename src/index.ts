import {name as _delay} from './utility/src/delay';
import {name as _formatNumber} from './utility/src/formatNumber';

export const delay = _delay;
export const formatNumber = _formatNumber;
export * as tools from './utility';