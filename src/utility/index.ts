import {name as _delay} from './src/delay';
import {name as _formatNumber} from './src/formatNumber';
import {name as _percentage} from './src/percentage';

export const delay = _delay;
export const formatNumber = _formatNumber;
export const percentage = _percentage;